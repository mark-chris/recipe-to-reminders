# Build output
/lambda
/bootstrap
bootstrap.zip

# Lambda layers
layers/tesseract/tesseract-layer.zip

# Test artifacts
coverage.out
*.test

# Secrets — never commit these
.env
.env.*
*.pem
*.key
credentials.json
terraform.tfvars
terraform.tfstate
terraform.tfstate.backup
.terraform/
.terraform.lock.hcl

# OS
.DS_Store
Thumbs.db
